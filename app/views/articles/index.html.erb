<%= render "hero" %>

<div class="bg-gray-100 py-18">
  <div class="container mx-auto flex flex-wrap justify-center">
    <% @articles.each do |article| %>
      <%= render article %>
    <% end %>
    <div class="w-full my-3">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  </div>
</div>