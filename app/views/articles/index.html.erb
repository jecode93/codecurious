<%= render "hero" %>

<div class="bg-body-tertiary">
  <div class="container mx-auto py-5 row align-items-center">
    <% @articles.each do |article| %>
      <%= render article %>
    <% end %>
    <div class="my-3">
      <%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  </div>
</div>

